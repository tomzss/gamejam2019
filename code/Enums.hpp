#pragma once

namespace Layers{
enum Layers{
    background,
    player,
    animal,
    bullet,
    kabum
};
}

enum class Direction{
    left,
    right,
    up,
    down
};

enum class Materia{
    normal,
    anty
};

enum class Animal_type
{
    ryba,
    motyl,
    zolw
};
